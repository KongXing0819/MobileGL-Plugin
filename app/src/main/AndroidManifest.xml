<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <manifest package="com.mgdev.mobilegl">
    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@drawable/img_app"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="fclPlugin"
            android:value="true" />
        <meta-data
            android:name="des"
            android:value="MobileGL(OpenGL 3.2)(Stable:1.21.2+)" />
        <meta-data
            android:name="renderer"
            android:value="MobileGL:libMobileGL.so:libMobileGL.so" />
        <meta-data
            android:name="boatEnv"
            android:value="DLOPEN=libspirv-cross-c-shared.so:LIBGL_ES=3" />
        <meta-data
            android:name="pojavEnv"
            android:value="DLOPEN=libspirv-cross-c-shared.so:LIBGL_ES=3:POJAV_RENDERER=opengles3" />
        <meta-data
            android:name="minMCVer"
            android:value="1.17" />

    </application>

</manifest>